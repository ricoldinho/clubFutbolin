# Base del API desde el navegador.
# En desarrollo con `vite`: usar `/api` para que el proxy reescriba a http://localhost:3000 (sin CORS).
VITE_API_BASE=/api

# En preview/producción sin proxy, p.ej.:
# VITE_API_BASE=https://api.tudominio.com
